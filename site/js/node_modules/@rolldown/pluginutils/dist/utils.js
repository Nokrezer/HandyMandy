"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.cleanUrl = cleanUrl;
exports.extractQueryWithoutFragment = extractQueryWithoutFragment;
var postfixRE = /[?#].*$/;
function cleanUrl(url) {
  return url.replace(postfixRE, '');
}
function extractQueryWithoutFragment(url) {
  var questionMarkIndex = url.indexOf('?');
  if (questionMarkIndex === -1) {
    return '';
  }
  var fragmentIndex = url.indexOf('#', questionMarkIndex); // Search for # after ?
  if (fragmentIndex === -1) {
    return url.substring(questionMarkIndex);
  } else {
    return url.substring(questionMarkIndex, fragmentIndex);
  }
}